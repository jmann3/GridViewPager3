<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gridviewpager2"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="16" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Demo" 
        android:name="com.example.gridviewpager2.Globals">
        <activity
            android:name=".GridViewPager"
            android:theme="@style/Theme.Demo"
            android:label="@string/demo_pager2_title" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="GridImageActivity"></activity>
        <activity android:name="TopicActivity"></activity>
    </application>

</manifest>